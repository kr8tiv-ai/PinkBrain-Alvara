# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | EVM target chains | Base + Ethereum mainnet (fund creator chooses at setup) | Base has lower gas costs and fresh Alvara deployment (April 2026). Ethereum has deeper liquidity. User wants both. | No | collaborative |
| D002 | M001 | arch | Alvara integration approach | Reverse-engineer factory contracts from on-chain data, no team outreach | User explicitly chose "find it on chain" over contacting Alvara team. Avoids external dependency. | Yes — if on-chain discovery proves impossible due to MEV backend requirements | human |
| D003 | M001 | arch | Cross-chain bridge protocol | deBridge DLN as primary, Wormhole deferred as potential fallback | deBridge has zero TVL risk (intent-based), 1-2s fulfillment, simple REST API, zero exploit history. Wormhole fallback deferred to reduce integration surface. | Yes — if deBridge proves unreliable on Solana↔Base route | collaborative |
| D004 | M001 | arch | Divestment config mutability | Immutable after fund creation — stored on-chain | Trust model: holders verify config once and know it's permanent. Owner can only change basket composition, not distribution split or triggers. | No | human |
| D005 | M001 | arch | Multi-tenancy model | No hard limit on active funds — each fund gets isolated state, wallets, and job queues | The app is generic for any Bags.fm token creator, not $BRAIN-specific. Isolated failure domains prevent one fund's issues from affecting others. | Yes — may add practical caps based on infrastructure costs | human |
| D006 | M001 | arch | Revenue model | Protocol fee on reflections — deducted on Solana side before bridge | User chose protocol fee over setup fees or Alvara management fee only. Deducting on Solana side is simplest — one SPL transfer before the pipeline runs. | Yes — percentage configurable at platform level | human |
| D007 | M001 | arch | Distribution recipient model | Top 100 holders by token balance — no separate staking contract | User explicitly chose holders model. Simpler, aligns with Bags.fm existing top-100 dividend distribution model. | No | human |
| D008 | M004 | arch | App hosting model | Embedded in Bags.fm app store | User specifically wants it embedded, not a standalone web app. Bags.fm app store embed format needs investigation in M004. | Yes — if Bags.fm doesn't support embedding | human |
| D009 | M002 | arch | Accumulation scheduler architecture | Single cron job polling all active funds | Simpler than per-fund timers, fewer moving parts, easier to monitor and debug. One job iterates over all funds every 6 hours. | Yes — if fund count exceeds single-job processing window | collaborative |
| D010 | M002 | arch | Fund lifecycle model | Repeating cycles — accumulate → invest → divest → distribute → repeat | Funds run continuously. Each divestment cycle distributes proceeds, then the fund starts accumulating again. No terminal state unless owner explicitly shuts down. | No | collaborative |
| D011 | M002 | arch | On-chain divestment config storage | Single registry contract per chain (Base and Ethereum) | One contract maps fund ID → immutable config struct. Cheaper gas than per-fund contracts. Single source of truth per chain. | No | collaborative |
| D012 | M002 | arch | Alvara rebalance execution path | Route through Alvara's interface — reverse-engineer their backend API calls | Alvara's MEV protection uses a backend signing service for swap routes. Direct contract calls may bypass this protection and result in worse execution. Replicating their signed flow preserves MEV protection. | Yes — if M001/S01 reveals direct contract calls work fine for all operations | collaborative |
| D013 | M002 | arch | Pipeline failure recovery strategy | Resume from failed step — each pipeline step is checkpointed | No wasted gas on completed steps. Each step records its completion state. On failure, retry from the exact failed step with exponential backoff. | No | collaborative |
| D014 | M003 | arch | Holder snapshot timing for divestment | Snapshot at trigger time (before redemption + bridge) | Prevents gaming by buying tokens during the bridge delay window. Snapshot is locked when divestment fires. | No | collaborative |
| D015 | M003 | arch | Distribution failure handling | Distribute → immediately verify on-chain → fix failures → verify fix. Tight loop, no held remainder. | User wants immediate verification and correction, not a deferred retry queue. Each transfer is verified on Solana, failures are retried with adjusted parameters, and the fix is verified before moving on. | No | human |
| D016 | M003 | arch | Minimum divestment threshold | Configurable per fund — skip divestment if basket TVL is below threshold | Gas + bridge fees on small amounts can eat the proceeds. Default minimum prevents uneconomical divestments. | Yes — default value adjustable | collaborative |
| D017 | M004 | convention | UI design language | Match Bags.fm website and app store branding — seamless, very clean | User wants the app to feel native to Bags.fm, not a separate product. Branded consistently with the platform it lives in. | No | human |
| D018 | M004 | arch | Anti-rug and anti-gaming protections | Immutable divestment config, on-chain verifiability, holder snapshot at trigger time, Alvara-managed basket operations | User explicitly said "can't be rugged or gamed." Immutable config prevents owner from changing split. On-chain transparency lets holders verify. Trigger-time snapshot prevents buy-in gaming. Alvara handles basket custody. | No | human |
| D019 | M001/S01/T01 | arch | Block explorer API for Base chain | Blockscout free API as primary, Etherscan V2 as optional fallback when API key is available | Etherscan deprecated V1 endpoints (Basescan) and V2 requires a paid API key for non-Ethereum chains. Blockscout provides the same data for free with no key requirement. The basescan.ts helper abstracts both backends. | Yes — if Etherscan offers free Base access or Blockscout has reliability issues | agent |
| D020 |  | architecture | How to model Alvara BSKT interface for verification | Use actual on-chain Alvara BSKT ABI (ERC-721 + custom getTokenDetails/totalTokens/getOwner) instead of standard ERC-7621 interface | Alvara BSKTs on Base do not implement standard ERC-7621 — supportsInterface(0xc9c80f73) returns false. BSKTs are ERC-721 NFTs deployed as beacon proxies with custom view functions. The actual ABI was discovered via the beacon implementation chain: factory.bsktImplementation() → UpgradeableBeacon.implementation() → logic contract verified on Blockscout. | Yes | agent |
| D021 |  | architecture | MEV protection integration strategy for BSKT creation | Confirmed backend-signed swap routes required via on-chain analysis of 5 recent createBSKT transactions; integration path requires Alvara backend API | All 5 decoded createBSKT transactions contain 65-byte ECDSA signatures and non-empty swapData arrays (1362-1810 bytes). The factory validates these signatures on-chain (InvalidSignature error in ABI). Direct calls with empty signature/swapData will revert. Alvara's backend computes optimal swap routes via 1inch and signs them for MEV protection. | Yes | agent |
